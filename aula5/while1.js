/* Ler 2 números até o usuário informar que deseja sair do programa.
*/

let continua = ['s', 'sim'];
let num1, num2;
let operacao;

while (continua == 's', 'sim') {
    num1 = parseInt(prompt('Numero 1: '));
    num2 = parseInt(prompt('Numero 2: '));
    operacao = prompt('operacao <+,-,*,/>');
    continua;
}